import {
  Meteor
} from 'meteor/meteor';

import {
  Channels
} from '../imports/api/channels.js'; 


import {
  Messages
} from '../imports/api/messages.js'; 

Meteor.startup(() => {
  // code to run on server at startup
});
